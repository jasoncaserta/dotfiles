# RTK - Rust Token Killer

**Usage**: Token-optimized CLI proxy for shell commands.

## Meta Commands

```bash
rtk gain
rtk gain --history
rtk discover
rtk proxy <cmd>
```

## Verification

```bash
rtk --version
rtk gain
which rtk
```

Supported shell commands are automatically rewritten by the Gemini CLI hook.
