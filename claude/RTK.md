# RTK - Rust Token Killer

**Usage**: Token-optimized CLI proxy for shell commands.

## Meta Commands

```bash
rtk gain
rtk gain --history
rtk discover
rtk proxy <cmd>
```

## Verification

```bash
rtk --version
rtk gain
which rtk
```

All other supported shell commands are automatically rewritten by the Claude Code hook.
